const baseNodeUrl = "http://127.0.0.1:3000";
const appointmentsUrl = `${baseNodeUrl}/appointments`;
const prescriptionsUrl = `${baseNodeUrl}/prescriptions`;
const clinicalConditionsUrl = `${baseNodeUrl}/clinical-conditions`


export { baseNodeUrl, appointmentsUrl, prescriptionsUrl, clinicalConditionsUrl };